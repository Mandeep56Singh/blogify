spring:
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    locations: classpath:db/migration

  logging:
    level:
      root: WARN
      com.mandeep.blogify: INFO
      org.springframework: WARN
      org.hibernate.SQL: ERROR

