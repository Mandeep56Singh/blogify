spring:
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true


    locations: classpath:db/migration

  logging:
    level:
      # Set this to DEBUG if you want to see SQL queries during tests
      org.hibernate.orm.jdbc.bind: DEBUG

